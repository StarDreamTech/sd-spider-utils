# 复制为 codex.env，填入 auth.json 中 tokens 下的对应值。
# 单账号：
CODEX_ACCESS_TOKEN=replace-with-access-token
CODEX_ACCOUNT_ID=replace-with-account-id

# 多账号：同一账号的两个键使用相同名称（字母、数字或下划线）。
# CODEX_WORK_ACCESS_TOKEN=replace-with-work-access-token
# CODEX_WORK_ACCOUNT_ID=replace-with-work-account-id

# 也支持把账号名放在末尾（与上面的格式二选一，不重复定义同一账号字段）：
# CODEX_ACCESS_TOKEN_personal=replace-with-personal-access-token
# CODEX_ACCOUNT_ID_personal=replace-with-personal-account-id

# 任一值可使用 base64rev: 前缀，后接 Base64 编码后倒序的内容。
# 使用 sd-codex-usage --encode 生成完整值；这是混淆，不是加密。
